import Logo from "../Elements/Logo";

const Footer = () => {
  return (
    <footer className="container mt-10 p-8 rounded-t-3xl outline outline-primary-400 grid grid-cols-5">
      <Logo />
    </footer>
  );
};

export default Footer;
